class Node {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}


let moves = [(1, 2), (2, 1), (-1, 2), (1, -2), (-2, 1), (-2, -1), (-1, -2), (2, -1)]

function knightMoves(startingPoint, endPoint) {

}